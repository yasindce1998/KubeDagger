run:
  timeout: 5m
  build-tags:
    - linux

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gocritic
    - gosec
    - revive
    - bodyclose

issues:
  exclude-dirs:
    - ebpf
  exclude-rules:
    - path: pkg/kubedagger/
      linters:
        - errcheck
        - unused
        - gosec
        - gocritic
    - path: _test\.go
      linters:
        - gosec
        - gocritic
    - path: pkg/agent/modules/
      linters:
        - gosec
    - path: cmd/kubedagger-client/
      linters:
        - gocritic
    - path: cmd/kubedagger-operator/
      linters:
        - gocritic

linters-settings:
  errcheck:
    exclude-functions:
      - (io.Closer).Close
      - (*os.File).Close
  gosec:
    excludes:
      - G104
      - G304
      - G402
  revive:
    rules:
      - name: exported
        disabled: true
  gocritic:
    disabled-checks:
      - ifElseChain
      - assignOp
      - singleCaseSwitch
      - unslice
      - commentFormatting
      - dupBranchBody
